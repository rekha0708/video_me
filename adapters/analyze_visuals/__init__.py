"""Source-video visual analysis: sample frames and describe on-screen settings.

Grounds the invented scene settings in what the source video actually shows, so
the rendered backgrounds match the real room/garden/etc. instead of a generic
LLM guess. Skipped for story-input jobs (no video).
"""
